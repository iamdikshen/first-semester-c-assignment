// write a program to calculate the surface area of a cube 
#include <stdio.h>
int main(int argc, char const *argv[])
{
    int L;
    printf("enter the length of cube\n");
    scanf("%d",&L);
    printf("the surface area of the cube is %d",6*L*L);
    return 0;
}
